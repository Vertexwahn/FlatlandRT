load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `touch`

build_test(
    name = "touch",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:touch"],
    visibility = ["//:__subpackages__"],
)
