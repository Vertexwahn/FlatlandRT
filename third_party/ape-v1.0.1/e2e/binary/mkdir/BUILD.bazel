load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `mkdir`

build_test(
    name = "mkdir",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:mkdir"],
    visibility = ["//:__subpackages__"],
)
