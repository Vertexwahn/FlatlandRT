load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `emacsclient`

build_test(
    name = "emacsclient",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:emacsclient"],
    visibility = ["//:__subpackages__"],
)
