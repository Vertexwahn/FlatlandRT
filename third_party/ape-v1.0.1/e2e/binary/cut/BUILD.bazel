load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `cut`

build_test(
    name = "cut",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:cut"],
    visibility = ["//:__subpackages__"],
)
