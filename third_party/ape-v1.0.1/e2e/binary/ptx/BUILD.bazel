load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `ptx`

build_test(
    name = "ptx",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:ptx"],
    visibility = ["//:__subpackages__"],
)
