load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `groups`

build_test(
    name = "groups",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:groups"],
    visibility = ["//:__subpackages__"],
)
