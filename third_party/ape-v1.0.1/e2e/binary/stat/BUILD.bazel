load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `stat`

build_test(
    name = "stat",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:stat"],
    visibility = ["//:__subpackages__"],
)
