load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `expand`

build_test(
    name = "expand",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:expand"],
    visibility = ["//:__subpackages__"],
)
