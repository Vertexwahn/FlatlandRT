load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `nproc`

build_test(
    name = "nproc",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:nproc"],
    visibility = ["//:__subpackages__"],
)
