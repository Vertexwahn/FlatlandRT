load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `whois`

build_test(
    name = "whois",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:whois"],
    visibility = ["//:__subpackages__"],
)
