load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `tmux`

build_test(
    name = "tmux",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:tmux"],
    visibility = ["//:__subpackages__"],
)
