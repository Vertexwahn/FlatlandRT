load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `uptime`

build_test(
    name = "uptime",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:uptime"],
    visibility = ["//:__subpackages__"],
)
