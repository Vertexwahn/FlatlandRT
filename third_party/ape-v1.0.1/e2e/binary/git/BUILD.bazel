load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `git`

build_test(
    name = "git",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:git"],
    visibility = ["//:__subpackages__"],
)
