load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `chroot`

build_test(
    name = "chroot",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:chroot"],
    visibility = ["//:__subpackages__"],
)
