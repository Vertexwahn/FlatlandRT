load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `dircolors`

build_test(
    name = "dircolors",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:dircolors"],
    visibility = ["//:__subpackages__"],
)
