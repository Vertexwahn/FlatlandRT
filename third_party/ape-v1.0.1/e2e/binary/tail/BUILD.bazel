load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `tail`

build_test(
    name = "tail",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:tail"],
    visibility = ["//:__subpackages__"],
)
