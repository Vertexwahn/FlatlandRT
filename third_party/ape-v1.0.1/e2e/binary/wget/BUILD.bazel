load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `wget`

build_test(
    name = "wget",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:wget"],
    visibility = ["//:__subpackages__"],
)
