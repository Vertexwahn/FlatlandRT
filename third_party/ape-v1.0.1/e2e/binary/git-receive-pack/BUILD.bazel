load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `git-receive-pack`

build_test(
    name = "git-receive-pack",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:git-receive-pack"],
    visibility = ["//:__subpackages__"],
)
