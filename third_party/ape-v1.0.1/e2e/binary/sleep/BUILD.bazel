load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `sleep`

build_test(
    name = "sleep",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:sleep"],
    visibility = ["//:__subpackages__"],
)
