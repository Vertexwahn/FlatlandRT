load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `ctags`

build_test(
    name = "ctags",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:ctags"],
    visibility = ["//:__subpackages__"],
)
