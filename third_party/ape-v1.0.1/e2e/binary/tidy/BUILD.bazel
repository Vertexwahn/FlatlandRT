load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `tidy`

build_test(
    name = "tidy",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:tidy"],
    visibility = ["//:__subpackages__"],
)
