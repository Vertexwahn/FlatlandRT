load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `nesemu1`

build_test(
    name = "nesemu1",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:nesemu1"],
    visibility = ["//:__subpackages__"],
)
