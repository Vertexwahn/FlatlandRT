load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `cpuid`

build_test(
    name = "cpuid",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:cpuid"],
    visibility = ["//:__subpackages__"],
)
