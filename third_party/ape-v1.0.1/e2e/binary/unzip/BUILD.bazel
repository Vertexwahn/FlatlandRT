load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `unzip`

build_test(
    name = "unzip",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:unzip"],
    visibility = ["//:__subpackages__"],
)
