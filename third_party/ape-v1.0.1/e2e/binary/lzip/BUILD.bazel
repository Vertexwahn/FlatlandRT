load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `lzip`

build_test(
    name = "lzip",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:lzip"],
    visibility = ["//:__subpackages__"],
)
