load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `readlink`

build_test(
    name = "readlink",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:readlink"],
    visibility = ["//:__subpackages__"],
)
