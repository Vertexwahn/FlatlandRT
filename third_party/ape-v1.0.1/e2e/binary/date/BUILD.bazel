load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `date`

build_test(
    name = "date",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:date"],
    visibility = ["//:__subpackages__"],
)
