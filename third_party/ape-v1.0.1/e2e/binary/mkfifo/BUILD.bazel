load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `mkfifo`

build_test(
    name = "mkfifo",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:mkfifo"],
    visibility = ["//:__subpackages__"],
)
