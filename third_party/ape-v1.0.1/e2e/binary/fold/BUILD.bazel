load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `fold`

build_test(
    name = "fold",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:fold"],
    visibility = ["//:__subpackages__"],
)
