load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `pr`

build_test(
    name = "pr",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:pr"],
    visibility = ["//:__subpackages__"],
)
