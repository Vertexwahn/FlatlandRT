load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `basename`

build_test(
    name = "basename",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:basename"],
    visibility = ["//:__subpackages__"],
)
