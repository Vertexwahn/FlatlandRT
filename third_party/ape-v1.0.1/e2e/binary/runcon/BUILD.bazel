load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `runcon`

build_test(
    name = "runcon",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:runcon"],
    visibility = ["//:__subpackages__"],
)
