load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `git-shell`

build_test(
    name = "git-shell",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:git-shell"],
    visibility = ["//:__subpackages__"],
)
