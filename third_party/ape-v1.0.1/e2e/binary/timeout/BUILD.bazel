load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `timeout`

build_test(
    name = "timeout",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:timeout"],
    visibility = ["//:__subpackages__"],
)
