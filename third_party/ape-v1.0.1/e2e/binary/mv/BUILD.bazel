load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `mv`

build_test(
    name = "mv",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:mv"],
    visibility = ["//:__subpackages__"],
)
