load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `uniq`

build_test(
    name = "uniq",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:uniq"],
    visibility = ["//:__subpackages__"],
)
