load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `mktemper`

build_test(
    name = "mktemper",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:mktemper"],
    visibility = ["//:__subpackages__"],
)
