load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `emacs`

build_test(
    name = "emacs",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:emacs"],
    visibility = ["//:__subpackages__"],
)
