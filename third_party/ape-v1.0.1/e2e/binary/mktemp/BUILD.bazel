load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `mktemp`

build_test(
    name = "mktemp",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:mktemp"],
    visibility = ["//:__subpackages__"],
)
