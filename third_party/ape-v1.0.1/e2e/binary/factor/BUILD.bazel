load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `factor`

build_test(
    name = "factor",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:factor"],
    visibility = ["//:__subpackages__"],
)
