load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `vdir`

build_test(
    name = "vdir",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:vdir"],
    visibility = ["//:__subpackages__"],
)
