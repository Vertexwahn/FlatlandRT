load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `sync`

build_test(
    name = "sync",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:sync"],
    visibility = ["//:__subpackages__"],
)
