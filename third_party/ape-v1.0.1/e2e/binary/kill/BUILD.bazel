load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `kill`

build_test(
    name = "kill",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:kill"],
    visibility = ["//:__subpackages__"],
)
