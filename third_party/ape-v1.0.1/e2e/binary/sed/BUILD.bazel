load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `sed`

build_test(
    name = "sed",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:sed"],
    visibility = ["//:__subpackages__"],
)
