load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `comm`

build_test(
    name = "comm",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:comm"],
    visibility = ["//:__subpackages__"],
)
