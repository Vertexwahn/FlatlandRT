load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `tee`

build_test(
    name = "tee",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:tee"],
    visibility = ["//:__subpackages__"],
)
