load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `scrapscript`

build_test(
    name = "scrapscript",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:scrapscript"],
    visibility = ["//:__subpackages__"],
)
