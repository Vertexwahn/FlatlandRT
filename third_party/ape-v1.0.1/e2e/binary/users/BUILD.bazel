load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `users`

build_test(
    name = "users",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:users"],
    visibility = ["//:__subpackages__"],
)
