load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `dirname`

build_test(
    name = "dirname",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:dirname"],
    visibility = ["//:__subpackages__"],
)
