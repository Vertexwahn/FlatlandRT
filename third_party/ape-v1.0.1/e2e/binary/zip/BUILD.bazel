load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `zip`

build_test(
    name = "zip",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:zip"],
    visibility = ["//:__subpackages__"],
)
