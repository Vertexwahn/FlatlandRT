load("@bazel_skylib//rules:build_test.bzl", "build_test")

# TODO: write an _actual_ test for `unlink`

build_test(
    name = "unlink",
    size = "small",
    tags = ["stub"],
    targets = ["@ape//ape:unlink"],
    visibility = ["//:__subpackages__"],
)
